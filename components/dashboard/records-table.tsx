export function RecordsTable({title,rows,columns,empty}:{title:string;rows:Record<string,unknown>[];columns:{key:string;label:string;format?:(value:unknown,row:Record<string,unknown>)=>React.ReactNode}[];empty:string}){return <section className="finder-card"><div className="finder-card-head"><h2>{title}</h2><span className="evidence-badge">{rows.length} records</span></div>{rows.length?<div className="finder-table-wrap"><table className="finder-table"><thead><tr>{columns.map(c=><th key={c.key}>{c.label}</th>)}</tr></thead><tbody>{rows.map((row,index)=><tr key={String(row.id||index)}>{columns.map(c=><td data-label={c.label} key={c.key}>{c.format?c.format(row[c.key],row):String(row[c.key]??"Not yet verified")}</td>)}</tr>)}</tbody></table></div>:<div className="finder-empty"><strong>No records yet</strong><p>{empty}</p></div>}</section>}
