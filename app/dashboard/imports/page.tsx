import { CsvImporter } from "@/components/imports/csv-importer";import { requireSession } from "@/lib/auth/session";
export default async function ImportsPage(){const profile=await requireSession();return <><div className="finder-page-head"><div><p className="finder-eyebrow">Authorized data intake</p><h1>CSV property imports</h1><p>Preview, validate, detect source-ID duplicates, dry run, and retain an import report. No scraping or automatic listing feeds are active.</p></div></div>{profile.role==="admin"?<CsvImporter/>:<div className="finder-empty"><strong>Administrator access required</strong><p>Analysts and viewers can review imported records but cannot run bulk imports.</p></div>}</>}
