import { RecordsTable } from "@/components/dashboard/records-table";import { getCollection } from "@/lib/site-finder-data";
export default async function ProjectsPage(){const rows=await getCollection("projects") as Record<string,unknown>[];return <><div className="finder-page-head"><div><p className="finder-eyebrow">Formal project portfolio</p><h1>Solar projects</h1><p>Projects converted from candidate properties with milestones, commercial status, and conceptual values clearly labeled.</p></div></div><RecordsTable title="Project records" rows={rows} empty="Convert an administrator-approved candidate property to create a linked project." columns={[{key:"project_code",label:"Code"},{key:"project_name",label:"Project"},{key:"project_stage",label:"Stage"},{key:"proposed_capacity_mw_dc",label:"Conceptual MW DC"},{key:"annual_generation_estimate_kwh",label:"Conceptual annual kWh",format:v=>typeof v==="number"?v.toLocaleString():String(v??"Not yet verified")},{key:"interconnection_status",label:"Interconnection"},{key:"target_cod",label:"Target COD"}]}/></>}
