import { CapitalWorkspace } from "@/components/finance/capital-workspace";import { requireRole } from "@/lib/auth/session";export default async function CapitalPage(){await requireRole(["owner","admin"]);return <CapitalWorkspace view="overview"/>}
