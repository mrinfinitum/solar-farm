import { CapitalWorkspace } from "@/components/finance/capital-workspace";import { requireRole } from "@/lib/auth/session";export default async function CapitalTermSheetsPage(){await requireRole(["owner","admin"]);return <CapitalWorkspace view="term-sheets"/>}
