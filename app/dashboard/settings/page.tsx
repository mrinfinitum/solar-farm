import { LockKeyhole, Settings2, ShieldCheck } from "lucide-react";import { TeamInvite } from "@/components/dashboard/team-invite";import { requireSession } from "@/lib/auth/session";import { initialTargetProfile, scoreCategories, scoringWeights } from "@/lib/scoring/config";import { providerRegistry } from "@/lib/integrations/registry";
export default async function SettingsPage(){const profile=await requireSession();const admin=profile.role==="admin";return <><div className="finder-page-head"><div><p className="finder-eyebrow">Workspace controls</p><h1>Settings and governance</h1><p>Team permissions, scoring configuration, target criteria, providers, document policy, exports, and protected administrative controls.</p></div><span className="evidence-badge">{profile.role} access</span></div><div className="finder-grid finder-two-col"><section className="finder-card finder-form-section"><h2>User profile</h2><p>Identity is managed by Supabase Auth. Profile edits remain constrained by row-level security.</p><div className="data-panel"><div><div><span>Name</span><strong>{profile.fullName}</strong></div><div><span>Email</span><strong>{profile.email}</strong></div><div><span>Role</span><strong>{profile.role}</strong></div></div></div></section><section className="finder-card finder-form-section"><h2>Team members</h2><p>Only administrators can invite users or change roles. Public registration remains disabled.</p>{admin?<TeamInvite/>:<button disabled className="finder-button"><LockKeyhole size={14}/>Administrator access required</button>}</section></div><section className="finder-card finder-form-section"><h2>Scoring configuration</h2><p>Versioned 100-point model. Changes should create a new version rather than rewriting prior calculations.</p><div className="settings-weight-grid">{Object.entries(scoringWeights).map(([key,value])=><div key={key}><span>{key.replace(/([A-Z])/g," $1")}</span><strong>{value} pts</strong></div>)}</div><div className="threshold-row">{scoreCategories.map(item=><span key={item.label}>{item.min}+ · {item.label}</span>)}</div><button disabled={!admin} className="finder-button"><Settings2 size={14}/>Edit weights</button></section><section className="finder-card finder-form-section"><h2>{initialTargetProfile.name}</h2><p>{initialTargetProfile.notes}</p><pre>{JSON.stringify(initialTargetProfile,null,2)}</pre><button disabled={!admin} className="finder-button">Edit target profile</button></section><section className="finder-card finder-form-section"><h2>Source and provider settings</h2><p>Only manual entry and authorized CSV import are active. Stubs return no data and make no integration claims.</p><div className="provider-list">{providerRegistry.map(provider=><div className="provider-row" key={provider.key}><div><strong>{provider.name}</strong><p>{provider.description}</p></div><span className={`finder-status ${provider.enabled?"finder-status--good":""}`}>{provider.enabled?"Available": "Not configured"}</span><p>{provider.licensingNotes}</p></div>)}</div></section><div className="finder-grid finder-two-col"><section className="finder-card finder-form-section"><h2>Document policy</h2><p>Private bucket · 15 MB maximum · PDF, PNG, JPG, CSV, XLSX, and DOCX · signed server-side downloads.</p><span className="evidence-positive"><ShieldCheck size={14}/>Public bucket access is disabled by migration policy.</span></section><section className="finder-card finder-form-section"><h2>Data export and danger zone</h2><p>Exports and destructive actions are restricted to administrators. Permanent deletion always requires explicit confirmation.</p><button disabled={!admin} className="finder-button">Request data export</button> <button disabled={!admin} className="finder-button">Administrative actions</button></section></div></>}
