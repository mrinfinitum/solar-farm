import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { propertyInputSchema } from "@/lib/validation/site-finder";
export async function POST(request:Request){const supabase=await createClient();if(!supabase)return NextResponse.json({error:"Supabase is not configured."},{status:503});const{data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:"Unauthorized"},{status:401});const{data:profile}=await supabase.from("profiles").select("role").eq("id",user.id).single();if(profile?.role!=="admin")return NextResponse.json({error:"Only administrators can import property records."},{status:403});const body=await request.json() as {sourceName?:string;fileName?:string;dryRun?:boolean;records?:Record<string,string>[]};if(!Array.isArray(body.records)||body.records.length>500)return NextResponse.json({error:"Imports must contain between 1 and 500 rows."},{status:422});let created=0,updated=0,rejected=0;const errors:Record<string,string>[]=[];for(let index=0;index<body.records.length;index++){const row=body.records[index];const parsed=propertyInputSchema.safeParse({property_code:row.property_code||`IMP-${Date.now()}-${index+1}`,project_name:row.project_name||row.address||null,status:"new",pipeline_stage:"discovery",source_type:"csv",source_name:row.source_name||body.sourceName||"CSV import",source_url:row.source_url||null,source_listing_id:row.source_listing_id||null,source_collected_at:row.source_collected_at||new Date().toISOString().slice(0,10),address_line_1:row.address,city:row.city,county:row.county,state:row.state||"Oklahoma",postal_code:row.postal_code||"",latitude:row.latitude||null,longitude:row.longitude||null,acreage_total:row.acreage_total||null,acreage_usable_estimate:null,asking_price:row.asking_price||null,property_type:row.property_type||null,current_land_use:row.current_land_use||null,tillable_status:row.tillable_status||null,cleared_percentage:row.cleared_percentage||null,wooded_percentage:row.wooded_percentage||null,slope_average_percent:null,legal_access_status:null,seller_financing_available:row.seller_financing_available||null,lease_option_possible:null,purchase_possible:null,owner_name:row.owner_name||null,broker_name:row.broker_name||null,internal_summary:row.notes||null,next_action:"Complete initial desktop screening",next_action_due_date:null});if(!parsed.success){rejected++;errors.push({row:String(index+2),property_code:row.property_code||"",error:parsed.error.issues.map(i=>`${i.path.join(".")}: ${i.message}`).join("; ")});continue}let existing:null|{id:string}=null;if(parsed.data.source_listing_id){const{data}=await supabase.from("properties").select("id").eq("source_name",parsed.data.source_name).eq("source_listing_id",parsed.data.source_listing_id).maybeSingle();existing=data}if(existing)updated++;else created++;if(!body.dryRun){const price_per_acre=parsed.data.asking_price&&parsed.data.acreage_total?parsed.data.asking_price/parsed.data.acreage_total:null;if(existing)await supabase.from("properties").update({...parsed.data,price_per_acre}).eq("id",existing.id);else await supabase.from("properties").insert({...parsed.data,price_per_acre,created_by:user.id})}}
  if(!body.dryRun)await supabase.from("imports").insert({import_type:"property-csv",source_name:body.sourceName||"CSV import",source_file_name:body.fileName||"upload.csv",imported_by:user.id,records_total:body.records.length,records_created:created,records_updated:updated,records_rejected:rejected,status:rejected?"completed-with-errors":"completed",error_log:errors,completed_at:new Date().toISOString()});return NextResponse.json({dryRun:Boolean(body.dryRun),created,updated,rejected,errors})}
